name: build test deploy webapp

on:  
  push:
    branches: [ main ]

env:
  AZURE_WEBAPP_NAME: webapp-1002
  AZURE_WEBAPP_PACKAGE_PATH: '.'

jobs:
  build-test-deploy:
    
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.101
    
    - name: Restore dependencies
      run: dotnet restore src\AspnetWebapiAzureDeploy.WebApp\AspnetWebapiAzureDeploy.WebApp.csproj
    
    - name: Build
      run: dotnet build src\AspnetWebapiAzureDeploy.WebApp\AspnetWebapiAzureDeploy.WebApp.csproj --configuration Release --no-restore
    
    - name: Test
      run: dotnet test test\AspnetWebapiAzureDeploy.WebAppTest\AspnetWebapiAzureDeploy.WebAppTest.csproj --verbosity normal --configuration Release

    - name: Publish
      run: dotnet publish src\AspnetWebapiAzureDeploy.WebApp\AspnetWebapiAzureDeploy.WebApp.csproj --configuration Release -o '${{ env.AZURE_WEBAPP_PACKAGE_PATH }}/webapp'

    - name: 'Deploy to Azure WebApp'
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        package: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}/webapp